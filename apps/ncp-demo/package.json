{
  "name": "@nextclaw/ncp-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "pnpm -C backend dev",
    "dev:frontend": "pnpm -C frontend dev",
    "build": "pnpm -C backend build && pnpm -C frontend build",
    "lint": "pnpm -C backend lint && pnpm -C frontend lint && eslint scripts",
    "tsc": "pnpm -C backend tsc && pnpm -C frontend tsc",
    "smoke": "node scripts/smoke.mjs",
    "smoke:ui": "node scripts/smoke-ui.mjs"
  },
  "devDependencies": {
    "eslint": "^9.39.3"
  }
}

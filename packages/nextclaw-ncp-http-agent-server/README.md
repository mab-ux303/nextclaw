# @nextclaw/ncp-http-agent-server

HTTP/SSE transport adapter for exposing an `NcpAgentServerEndpoint` over Hono routes.

## Build

```bash
pnpm -C packages/nextclaw-ncp-http-agent-server build
```

## API

- `createNcpHttpAgentRouter(options)`
- `mountNcpHttpAgentRoutes(app, options)`
